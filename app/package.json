{
  "name": "schedule-assistant-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "build": "vite build --configLoader runner",
    "preview": "vite preview --configLoader runner --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
